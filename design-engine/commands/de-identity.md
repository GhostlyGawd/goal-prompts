---
description: Run the visual-identity skill — derive a validated palette/type/motion/mark system as a new brand.json version, proven by specimen + contrast gates.
---

Load and follow the `visual-identity` skill from this plugin
(design-engine/skills/visual-identity/SKILL.md). Work in
design-engine/brand.json; after every change run brand_lint.py and
contrast.py; regenerate specimen.py. End at the operator gate with the
specimen and contrast report — apply nothing to product surfaces. $ARGUMENTS
